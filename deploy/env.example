# contac environment file
# Install to: ~/.config/contac/env
# Permissions: chmod 600 ~/.config/contac/env
#
# The node reads CONTAC_PASSPHRASE on startup to decrypt its private key.
# Keep this file private — it unlocks your encrypted store.

CONTAC_PASSPHRASE=your-passphrase-here
