# contacc environment file
# Install to: ~/.config/contacc/env
# Permissions: chmod 600 ~/.config/contacc/env
#
# The node reads CONTACC_PASSPHRASE on startup to decrypt its private key.
# Keep this file private — it unlocks your encrypted store.

CONTACC_PASSPHRASE=your-passphrase-here
